<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Movie</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-6">

<div class="max-w-lg mx-auto bg-white rounded-lg shadow-md p-6">
    <h2 class="text-2xl font-bold text-center text-red-600 mb-4">Update Movie</h2>

    <form action="/update/<%= product.id %>" enctype="multipart/form-data" method="post">
        <div class="mb-4">
            <img src="<%= product.image %>" class="w-24 h-24 mx-auto rounded-md mb-4" alt="Movie Image">
        </div>
        <div class="mb-4">
            <label for="name" class="block text-sm font-medium text-gray-700">Movie Name</label>
            <input type="text" id="name" name="movieName" value="<%= product.movieName %>" placeholder="Enter Name" class="mt-1 block w-full p-2 border border-gray-300 rounded-md focus:ring focus:ring-red-500" required>
        </div>
        
        <div class="mb-4">
            <label for="desc" class="block text-sm font-medium text-gray-700">Description</label>
            <input type="text" id="desc" name="discription" value="<%= product.discription %>" placeholder="Enter Description" class="mt-1 block w-full p-2 border border-gray-300 rounded-md focus:ring focus:ring-red-500" required>
        </div>
        
        <div class="mb-4">
            <label for="price" class="block text-sm font-medium text-gray-700">Price</label>
            <input type="text" id="price" name="price" value="<%= product.price %>" placeholder="Enter Price" class="mt-1 block w-full p-2 border border-gray-300 rounded-md focus:ring focus:ring-red-500" required>
        </div>
        
        <div class="mb-4">
            <label for="image" class="block text-sm font-medium text-gray-700">Image</label>
            <input type="file" id="image" name="image" class="mt-1 block w-full text-sm text-gray-500 border border-gray-300 rounded-md cursor-pointer bg-gray-50 focus:outline-none focus:ring focus:ring-red-500">
        </div>
        
        <div class="text-center">
            <button type="submit" class="w-full bg-red-600 text-white font-semibold py-2 rounded-md hover:bg-red-700 transition duration-200">Edit</button>
        </div>
    </form>
</div>

</body>
</html>



<!-- movieName
discription
ReleaseDate
image -->